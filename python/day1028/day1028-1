#맴버벼수[속성] private형 _name, id
#getter[맴버변수 값을 외부로]: name, id
#setter[맴버변수 값을 외부에서 가져온다]: _name

#def get_name(parameter):

class Student:
    def __init__(self, name, id):#생선자, 초기변수와 객체 생성시 초기값 설정.
        self._name=name
        self.id = id

    def get_name(self):#맴버변수 값을 외부로
        return self._name
    def getid(self):
        return  self.id
    def set_name(self,name):#맴버변수값 웹에서 가져온다
        self._name=name
    def __str__(self): #객체명을 출력에 이용할 경우의 사용할 명령 설정.
        return self._name + ", " + str(self.id)
    # overloading : 동일기능이지만 매개변수의 형식의 차이-파이썬 지원x
    # overrding : 상속. 부믜 함수를 자식에서 변겨해서 사용.

s1=Student('kim', 100)#객체변수=클래스명(생성자에 필요한 매개변수)
#s1.id=200
print(s1._name)
print(s1.get_name())
s1.set_name('hong')
print(s1)
